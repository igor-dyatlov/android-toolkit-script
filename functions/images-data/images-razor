#!/bin/bash

function razor-JSS15R {
JSS15R="razor-jss15r-factory-ec2d4f76.tgz"
  if [ -f "$JSS15R" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-jss15r*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-jss15r
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-jss15r*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-jss15r-factory-ec2d4f76.tgz
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-jss15r*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-jss15r
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-jss15r*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

function razor-KRT16S {
KRT16S="razor-krt16s-factory-7235eb0d.tgz"
  if [ -f "$KRT16S" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-krt16s
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-krt16s-factory-7235eb0d.tgz
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-krt16s
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

function razor-KTU84P {
KTU84P="razor-ktu84p-factory-b1b2c0da.tgz"
  if [ -f "$KTU84P" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-ktu84p
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-ktu84p-factory-b1b2c0da.tgz
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-ktu84p
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

function razor-LRX21P {
LRX21P="razor-lrx21p-factory-ba55c6ab.tgz"
  if [ -f "$LRX21P" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-lrx21p
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-lrx21p-factory-ba55c6ab.tgz
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-lrx21p
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

function razor-LRX22G {
LRX22G="razor-lrx22g-factory-bff2093e.tgz"
  if [ -f "$LRX22G" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-lrx22g*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-lrx22g
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-lrx22g*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-lrx22g-factory-bff2093e.tgz
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-lrx22g*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-lrx22g
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-lrx22g*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

function razor-LMY48T {
LMY48T="razor-lmy48t-factory-b6a844f2.tgz"
  if [ -f "$LMY48T" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-lmy48t*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-lmy48t
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-lmy48t*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-lmy48t-factory-b6a844f2.tgz
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-lmy48t*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-lmy48t
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-lmy48t*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

function razor-MRA58V {
MRA58V="razor-mra58v-factory-45d0421b.tgz"
  if [ -f "$MRA58V" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-mra58v*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-mra58v
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-mra58v*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-mra58v-factory-45d0421b.tgz
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-mra58v*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-mra58v
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-mra58v*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

function razor-MOB30X {
MOB30X="razor-mob30x-factory-52684dff.zip"
  if [ -f "$MOB30X" ]; then
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-mob30x*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-mob30x
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-mob30x*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  else
    # Download
    show_info 'Download Factory Images...'
    wget https://dl.google.com/dl/android/aosp/razor-mob30x-factory-52684dff.zip
    # Extract
    show_info 'Extraction Factory Images...'
    tar zxf razor-mob30x*.tgz
    # Install
    show_info 'Install Factory Images...'
    cd razor-mob30x
    ./flash-all.sh
    # Timeout
    sleep 5
    # Back
    cd -
    # Cleanup and finish
    rm -rf razor-mob30x*
    # Done
    show_success 'Done.'
    whiptail --title "Finished" --msgbox "Installation complete." 8 78
    images
  fi
}

# Nexus 7
function images-razor {
  eval `resize`
  NEXUS7W=$(whiptail \
    --notags \
    --title 'Nexus 7 [2013] (Wi-Fi)' \
    --menu '\nWhat is the firmware version you want to install?' \
    --ok-button 'Install' \
    --cancel-button 'Go Back' \
  $LINES $COLUMNS $(( $LINES - 12 )) \
    razor-JSS15R 'Jelly Bean 4.3' \
    razor-KRT16S 'KitKat 4.4' \
    razor-KTU84P 'KitKat 4.4.4' \
    razor-LRX21P 'Lollipop 5.0' \
    razor-LRX22G 'Lollipop 5.0.2' \
    razor-LMY48T 'Lollipop 5.1.1' \
    razor-MRA58V 'Marshmallow 6.0' \
    razor-MOB30X 'Marshmallow 6.0.1' \
  3>&1 1>&2 2>&3)

exitstatus=$?
  if [ $exitstatus = 0 ]; then
    clear && $NEXUS7W
  else
    clear && main
  fi
}
